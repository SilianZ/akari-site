---
description: 此模块可以控制其他模块
slug: /module
---

# module

-   强制开启

此模块可以控制其他模块。

## `module enable`

-   [仅管理员](permissions)
-   别名：`enable`

用法：

-   指定：`~module enable <模块 1> [<模块 2>...] [-g]`
-   全部（不推荐）：`~module enable all [-g]`

参数：

-   `-g`：应用至频道全局。

此命令可以开启指定或所有模块。

## `module disable`

-   [仅管理员](permissions)
-   别名：`disable`

用法：

-   指定：`~module disble <模块 1> [<模块 2>...] [-g]`
-   全部：`~module disable all [-g]`

参数：

-   `-g`：应用至频道全局。

此命令可以关闭指定或所有模块。

## `module list`

-   [仅管理员](permissions)
-   不推荐

用法：`~module list [legacy]`

此命令可以列出目前机器人的所有模块。

加入 `legacy` 强制使用文字模式。

## `module reload`

-   [仅超级用户](permissions)
-   别名：`reload`

用法：`~module reload <模块 1> [<模块 2>...]`

此命令可以重新加载模块。

## `module unload`

-   [仅超级用户](permissions)
-   别名：`unload`

用法：`~module unload <模块 1> [<模块 2>...]`

此命令可以卸载模块，并使其在重启时不加载。

## `module load`

-   [仅超级用户](permissions)
-   别名：`load`

用法：`~module load <模块 1> [<模块 2>...]`

此命令可以加载已卸载的模块。
