---
description: This module manages black whitelist of wiki sites
slug: /wiki_edit
---

# wiki_edit

-   [开源]\(/docs/help/faq#Why some modules are not open sources)
-   Depends：[wiki](wiki)

This module is used to manage the black and white list of wiki sites.

## `wiki_edit trust`

-   [仅超级用户](permissions)

Use：`~wiki_edit trust <wiki 域名/脚本路径>`

This command allows wiki sites to be whitelisted.

:::tip Script Path

"Script path" means a directory where the wiki is index.php, api.ph. ph. php.

-   On WMF websites (Wikipedia, WikiDictions), and Miraheze farms, this directory is `<Domain >/w/`.
-   On the English wiki of Fandom farm, this directory is in `<domain name>`.
-   On other languages of Fandom farm, this directory is in `<domain name>/<language code>/`.

If your wiki is self-constructed, or if your farm is not listed here, the following is the way to get a script path.

1.  Enter：`Special:Version` in the search box.
2.  After opening the page, slide down to find the second table.For Chinese wiki this form is called "Access Point URL".
3.  Find the second line of this table (in the case of Chinese for the wiki the table header of this line is “script path”) in the second column (in the case of Chinese the table in this column is “URL” in the case of Chinese wiki).Right-click this character and copy the link, that is the script path of this wiki.

:::

## `wiki_edit block`

-   [仅超级用户](permissions)

Use：\`~wiki_edit block \<wiki 域名/脚本路径>

This command can blacklist wiki sites.

## `wiki_edit registration`

-   [仅超级用户](permissions)

Use：`~wiki_edit ditrust <wiki 域名/脚本路径>`

This command can remove wiki sites from whitelist.

## `wiki_edit unblock`

-   [仅超级用户](permissions)

Use：\`~wiki_audit unblock \<wiki 域名/脚本路径>

This command can remove wiki sites from blacklist.

## `wiki_edit query`

-   [仅超级用户](permissions)

Use：\`~wiki_edit query \<wiki 域名/脚本路径>

This command allows robots to ask whether to access this wiki site.

## `wiki_edit list`

-   [仅超级用户](permissions)

Use：`~wiki_edit list`

This command allows you to view the existing black and white list list.
